\documentclass[tikz]{standalone}
%\usetikzlibrary{calc}
\begin{document}

\begin{tikzpicture}[scale=1.3, block/.style={rectangle, draw, minimum height=15mm, minimum width = 15mm}, sumnode/.style={circle, draw, inner sep=2pt}, node distance=22mm]
  \node[block,] (plant) {G(s)};
  \node[block, left of=plant, node distance=26mm] (controller) {F(s)};
  \node[sumnode, left of=controller,] (sum) {$\sum$};
  \node[coordinate, left of=sum,] (input) {};
  \node[coordinate, right of=plant] (measure) {};
  \node[coordinate, right of=measure] (output) {};


  \draw[->] (input) -- node[above, near start] {$y_{ref}$} (sum);
  \draw[->] (sum) -- node[above] {$e$} (controller);
  \draw[->] (controller) -- node[above] {$u$} (plant);
  \draw[->] (plant) --  node[above]{$y$} (output);
  \draw[->] (measure) -- ++(0,-2cm) -| (sum) node[left, pos=0.9] {$-$};

\end{tikzpicture}
\end{document}
