\documentclass[tikz]{standalone}
%\usetikzlibrary{calc}
\begin{document}

\begin{tikzpicture}[scale=1.3, block/.style={rectangle, draw, minimum height=10mm, minimum width = 12mm}, sumnode/.style={circle, draw, inner sep=2pt}, node distance=22mm]
  
  \node[sumnode] (sum) {$\sum$};
  \node[block, right of=sum] (G) {$\frac{1}{s}$};
  \node[block, below of=G, node distance=15mm] (K) {$K$};
  \node[coordinate, left of=sum,] (input) {};
  \node[coordinate, right of=G] (measure) {};
  \node[coordinate, right of=measure, node distance=10mm] (output) {};


  \draw[->] (input) -- node[above, near start] {$u$} (sum);
  \draw[->] (sum) -- node[above, ] {} (G);

  \draw[->] (G) -- node[above, near end] {$y$} (output);
  \draw[->] (measure) |- (K) -| node[left, very near end] {$-$} (sum);




\end{tikzpicture}
\end{document}
